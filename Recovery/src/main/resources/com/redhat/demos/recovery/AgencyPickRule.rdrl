package com.redhat.demos.recovery;

import java.lang.Number;

rule "AgencyPickRule"
	dialect "mvel"
	ruleflow-group "findagents"
	when
		Order( market in ( "1", "2", "3", "5" ) )
		agency : Agency( )
	then
		modify( agency ) {
				setName( "ABC Recovery" ), 
				setRating( 4 )
		}
end
