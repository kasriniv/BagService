import java.lang.Number;

rule "Apply Silver Status Promotion"
	dialect "mvel"
	ruleflow-group "apply-promotions"
	no-loop true
	when
		If a passenger has a flyer status of Silver
	then
		Waive Shipping Rates
end
